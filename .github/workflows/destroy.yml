name: Destroy Fittrack Infrastructure

on:
  workflow_dispatch: # Manual trigger 

jobs:
  destroy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: "eu-central-1"

      # Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Mask AWS Account ID
        run: echo "::add-mask::${{ secrets.AWS_ACCOUNT_ID }}"
      # deploy.yml and destroy.yml - add as first step in every job


      - name: Mask AWS Account ID
        run: echo "::add-mask::${{ secrets.AWS_ACCOUNT_ID }}"

      - name: Delete Compute Infrastructure
        run: |
            cd terraform/compute
            terraform init
            terraform destroy -auto-approve -var-file='values.tfvars'          

      - name: Delete foundational Infrastructure
        run: |
            cd terraform/foundational
            terraform init
            terraform destroy -auto-approve -var-file='values.tfvars'          